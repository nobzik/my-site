{
  "name": "my-site",
  "version": "1.0.0",
  "description": "Personal website with Vercel Analytics",
  "private": true,
  "scripts": {
    "dev": "npx http-server -p 3000",
    "build": "echo 'Static site - no build needed'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  }
}
